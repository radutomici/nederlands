<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}{{ irregular_verb.nlInfinitive }}{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
        <script src="https://verbix-api.nyc3.cdn.digitaloceanspaces.com/conjugator/v2.0.2.js"></script>
        <style>
            .pronoun { color: green; }
            .irregular { color: red; }
            .orto { color: blue; }
            .arca { color: purple; }
            .obsolete { color: gray; }
        </style>
    </head>
    <style>
        table, th, td {
            border:1px solid black;
        }
    </style>
    <body>
        <h2>{{ irregular_verb.nlInfinitive }}</h2>

        <table style="width:25%">
            <tr>
                <th scope="col">Infinitief</th>
                <td>{{ irregular_verb.nlInfinitive }}</td>
            </tr>
            <tr>
                <th scope="col">Imperfectum enkelvoud</th>
                <td>{{ irregular_verb.nlSimplePastSingular }}</td>
            </tr>
            <tr>
                <th scope="col">Imperfectum meervoud</th>
                <td>{{ irregular_verb.nlSimplePastPlural }}</td>
            </tr>
            <tr>
                <th scope="col">Perfectum</th>
                <td>{{ irregular_verb.nlPastParticiple }}</td>
            </tr>
            <tr>
                <th scope="col">Engels</th>
                <td>{{ irregular_verb.enInfinitive }}</td>
            </tr>
        </table>

        <h3>Present tense conjugation</h3>
        {% for form in conjugated_verb %}
            <tr>
                <li>
                    {{ form }}
                </li>
            </tr>
        {% endfor %}

    </body>
</html>